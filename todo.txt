
                ------------------- TODO -------------------

[] - Add settings icon & modal to support the view
[] - Add Logout to settings modal
[] - Add welcome page for new users, include functionality to support a one time view
[] - Add FAQ to settings page & modal to support the view, include a 'how to play'
[] - Add game timer loop to reward players
[] - Create an API endpoint to grab the user's AP (needs to update on blend click)
[] - Add notifications - Game timer loop, when the user cannot afford the cauldron, '-15 AP' on blend click
[] - Attach a reward to each element? e.g Find Lava - reward = 100


[[ LOOK AT EARLY DEPLOYMENT - THERE WILL BE PROBLEMS!! ]]


Implement increment animation for adding & removing currencies -> 

const animateValue =(obj, start, end, duration) => {
  let startTimestamp = null;
  const step = (timestamp) => {
    if (!startTimestamp) startTimestamp = timestamp;
    const progress = Math.min((timestamp - startTimestamp) / duration, 1);
    obj.innerHTML = Math.floor(progress * (end - start) + start);
    if (progress < 1) {
      window.requestAnimationFrame(step);
    }
  };
  window.requestAnimationFrame(step);
}

const obj = document.getElementById("value");
animateValue(obj, 0, 1000, 3000);
